const main = async (inputs, auths, event) => {
  // to get block inputs
  const item = inputs.item
  console.log('Your input is: ', item)

  // similarly to set block's output
  // core.setOutput('varName', value)
}

module.exports.main = main
